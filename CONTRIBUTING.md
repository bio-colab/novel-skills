# المساهمة — Contributing

مرحبًا بك في **حرفة الرواية / Novel Craft**. نرحّب بمساهمات المجتمع الأدبي ومطوري المصادر المفتوحة.

## مبادئ

1. **احترام التعدّد**: لا تُفرض بنية هوليوود على كل رواية. البنى اختيارية والعدسات التراثية متكافئة.
2. **توثيق كل شيء**: كل مهارة، كل وضع، كل قرار في العقل المُدير يجب أن يكون قابلاً للتتبع في `journal/`.
3. **مهارات موجزة**: نص المهارة (`SKILL.md`) موجّه للوكيل — واضح وقابل للتنفيذ، لا مقالًا نظريًا طويلًا (النظرية في `00-foundation/` و`docs/`).
4. **كود مرتّب**: بايثون للترتيب والملفات والواجهة؛ المنطق الأدبي العميق يبقى في المهارات/الوكيل.

## أنواع المساهمات المرحّب بها

| النوع | أين |
|-------|-----|
| مهارة جديدة أو تحسين مهارة | `skills/` |
| تحسين العقل المُدير | `novel_craft/orchestrator.py` + `docs/ORCHESTRATOR.md` |
| أسئلة مقابلة أفضل | `novel_craft/data/interview_script.yaml` |
| ترجمة / صياغة عربية أو إنجليزية | `docs/`, `README.md` |
| مراجع أدبية، عدسات تراث | `00-foundation/`, `skills/lens-*/` |
| اختبارات | `tests/` |

## سير العمل

1. افتح Issue يصف المشكلة أو الاقتراح.
2. فرّع من `main`.
3. نفّذ التغيير + حدّث التوثيق.
4. شغّل `python -m novel_craft --help` وتأكد أن الواجهة تعمل.
5. أرسل Pull Request يشرح: *لماذا* و*ماذا* و*كيف يُختبر*.

## معايير مهارة جديدة

- مجلد: `skills/<skill-id>/SKILL.md`
- Frontmatter: `name`, `description`, `layer`, `inputs`, `outputs`
- أقسام: الدور، المدخلات، الخطوات، قيود الجودة، ما لا تفعله، الروابط
- سجّل المهارة في `novel_craft/data/skills_registry.yaml`

## مدونة السلوك

- نقد النصوص لا نقد الأشخاص.
- لا تمييز على أساس لغة أو تقليد أدبي أو نوع روائي.
- افترض حسن النية؛ صحّح بالأدلة والمراجع.

## الترخيص

بالمساهمة، توافق على نشر عملك تحت رخصة MIT الخاصة بالمشروع.
